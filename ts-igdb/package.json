{
  "name": "ts-igdb",
  "files": [
    "dist", "src", "README.md", "proto/compiled.d.ts"
  ],
  "scripts": {
    "proto-to-ts": "ts-node scripts/proto-to-ts.ts",
    "build": "tsc --build --clean && tsc --project ./tsconfig.build.json",
    "prepublishOnly": "npm run proto-to-ts && npm run build"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/node": "^17.0.8",
    "axios": "^0.24.0",
    "protobufjs": "^6.11.2",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "ts-apicalypse": "^1.0.0"
  }
}
